---
layout: home
hero:
  name: "Iris"
  text: "Image facts with pixel-level proof"
  tagline: "Read screenshots and images with OCR boxes, layout, crops, and deterministic diffs."
  actions:
    - theme: brand
      text: Quickstart
      link: /guide/quickstart
    - theme: alt
      text: Tool reference
      link: /reference/tools
features:
  - title: Local-first
    details: The default path keeps source material on your machine and requires no API key.
  - title: Predictable work
    details: Fast geometry and layout are the default. OCR and local semantic helpers are explicit.
  - title: Citeable output
    details: Results carry source locators, routes, warnings, and gaps so agents can verify claims.
---

## Install

```bash
npx -y @sylphx/iris
```

Use `iris` from the CLI or connect the same tools to Claude Code, Codex, Cursor,
VS Code, and any MCP client.
