# Predictable defaults

`fast` returns deterministic geometry, metadata, and layout. `quality` explicitly
enables OCR and optional local semantic helpers. Generative captions and object
detection remain opt-in and never replace locators.
