[package]
name = "image-reader-mcp-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Rust-native MCP server for image-reader-mcp (rmcp)"

[lib]
path = "src/lib.rs"

[[bin]]
name = "iris-mcp-server"
path = "src/main.rs"

[dependencies]
anyhow = "1"
axum = "0.8"
image-reader-core = { path = "../image-reader-core" }
rmcp = { version = "2.1.0", features = ["server", "transport-io", "transport-streamable-http-server"] }
schemars = { version = "1", features = ["preserve_order"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
subtle = "2"
tokio = { version = "1", features = ["full"] }
tokio-util = { version = "0.7", features = ["rt"] }
